<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lost.customMapper.DetLostMapper">

	<!-- Lost共同sql语句 -->
	<sql id="Common_List">
		tl.id,tc.category_name AS categoryName,
		tu.username AS userName,
		tl.lost_place AS lostPlace,
		tl.rewards,tl.contacts,tl.email,
		tl.description,tl.infoTitle,
		tl.lost_time AS lostTime1
	</sql>

	<!-- 查询所有明细失物 -->
	<select id="selectAllDetailLost" resultType="com.lost.customPojo.DetLost">
		SELECT
			<include refid="Common_List" />
		FROM
			tb_lost AS tl
			INNER JOIN tb_category AS tc ON tl.category_id = tc.id
			INNER JOIN tb_users AS tu ON tl.user_id = tu.id
	</select>
	
	<!-- 按条件分页查询所有明细失物 -->
	<select id="selectConditionLost" resultType="com.lost.customPojo.DetLost" parameterType="com.lost.customPojo.DetLost">
		SELECT
			<include refid="Common_List" />
		FROM
			tb_lost AS tl
			INNER JOIN tb_users AS tu ON tl.user_id = tu.id
			INNER JOIN (
				SELECT
					id,category_name
				FROM
					tb_category
				<where>
					<if test="categoryName != null">
						 category_name = #{categoryName,jdbcType=VARCHAR}
					</if>
				</where>
			) AS tc ON tl.category_id = tc.id
		<where>
			<if test="lostPlace != null">
					tl.lost_place LIKE "%"#{lostPlace,jdbcType=VARCHAR}"%"
			</if>
			<if test="lostTime1 != null and lostTime2 != null">
					AND tl.lost_time BETWEEN #{lostTime1,jdbcType=TIMESTAMP} AND #{lostTime2,jdbcType=TIMESTAMP}
			</if>
		</where>
	</select>

</mapper>